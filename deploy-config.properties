# Deploy Configuration - Taniguchi
# This file is maintained per deploy/* branch and should NOT be merged back.

# Customer identifier
customer.name=taniguchi
customer.display_name=谷口

# Comma-separated list of preset API server URLs shown in Settings
preset.urls=http://10.0.2.2:8000,https://wms.sakemaru.click,https://wms.taniguchi.net

# Default API host (used when local.properties is absent, e.g. CI/MDM builds)
default.api.host=wms.taniguchi.net
